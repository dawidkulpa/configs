# PROJECT KNOWLEDGE BASE

**Generated:** 2026-04-04
**Commit:** 0d5b2f9
**Branch:** master

## OVERVIEW

Cross-platform personal dotfiles/configs repo. Three independent platform silos — changes never span across them. Primary user: `buggy`. Timezone: Europe/Warsaw, locale: en_GB + pl_PL.

## STRUCTURE

```
configs/
├── nix/          # NixOS/Linux — flake ecosystem, 4 machine configs (see nix/AGENTS.md)
├── macos/        # macOS — shell scripts + dotfiles (see macos/AGENTS.md)
└── boxstarter/   # Windows — Boxstarter/Chocolatey automation (see boxstarter/AGENTS.md)
```

## CROSS-PLATFORM NOTES

- Fish shell is the primary shell across all platforms
- Helix is the primary editor across all platforms
- Theme: Catppuccin Mocha (helix), gruvbox-dark (bat)
- Tool overlap between platforms: fish, helix, starship, btop, fzf, ripgrep, fd, eza, zoxide, bat
- macOS configs in `.config/` are static files; Nix configs are generated by home-manager
- No CI/CD — configs are applied manually per-machine
- No tests — validation is implicit (`nix flake check` for Nix, manual for others)

## CONVENTIONS

- **Formatter**: alejandra (Nix), deno (Markdown). Run `./nix/format.sh`
- **Git abbreviations**: prefixed with `,` (e.g., `,s` = `git status`, `,p` = `git push`)
- `cd` aliased to `o` — smart cd that goes to git root with no args, offers to create missing dirs
